@props(["pageTitle","pageDesc"])
<section class="bg-primary-custom text-white py-5">
    <div class="container text-center">
        <h1 class="fw-bold">{{ $pageTitle }}</h1>
        <p class="mb-0">{{ $pageDesc }}</p>
    </div>
</section>